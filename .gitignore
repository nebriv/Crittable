# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
# uv generates uv.lock when running ``uv sync`` locally. The repo
# does not currently pin a lockfile (decision deferred until we have
# a CI pipeline that needs reproducible installs); ignore the local
# artifact so a dev that ran ``uv sync`` doesn't accidentally land it
# in an unrelated PR.
uv.lock

# Node
node_modules/
dist/
.eslintcache
*.tsbuildinfo

# Editors / OS
.vscode/
.idea/
.DS_Store

# Local env
.env
.env.local

# Build artifacts
backend/app/static/
